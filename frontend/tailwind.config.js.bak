/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./src/**/*.{html,ts}",
    ],
    theme: {
        extend: {
            colors: {
                "primary": "#ffd1dc", // Blush Pink
                "background-light": "#fcf8f9", // Very light tint
                "background-dark": "#230f14",
                "accent-nude": "#F5E6CA", // Soft Nude
                "text-main": "#333333", // Deep Charcoal
                "accent-rose": "#b76e79", // Rose Gold-ish accent for text/icons
            },
            fontFamily: {
                "display": ["Manrope", "sans-serif"]
            },
            borderRadius: {
                "DEFAULT": "0.5rem",
                "lg": "0.75rem", // Approx 12px
                "xl": "1.5rem",
                "full": "9999px"
            },
        },
    },
    plugins: [
        require('@tailwindcss/container-queries'),
        require('@tailwindcss/forms'),
    ],
}
